import React from 'react';
import PackageMain from '../components/package/PackageMain';

const PackageList = () => {
    return (
        <div>
            <PackageMain />
        </div>
    );
};

export default PackageList;