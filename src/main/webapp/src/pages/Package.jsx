import React from 'react';
import PackagePage from '../components/package/PackagePage';

const Package = () => {
    return (
        <div>
            <PackagePage/>
        </div>
    );
};

export default Package;